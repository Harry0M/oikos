<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base config for the entire app -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Domain-specific configuration for Firebase -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebaseio.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">firebase.google.com</domain>
        <domain includeSubdomains="true">firebaseapp.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
        
        <!-- 
            SSL Certificate Pinning for Firebase domains
            Note: Google rotates certificates, so using backup pins is essential.
            Update these pins periodically or use public key pins for stability.
        -->
        <pin-set expiration="2025-12-31">
            <!-- Google Trust Services root certificates -->
            <pin digest="SHA-256">hxqRlPTu1bMS/0DITB1SSu0vd4u/8l8TjPgfaAp63Gc=</pin>
            <pin digest="SHA-256">Vjs8r4z+80wjNcr1YKepWQboSIRi63WsWXhIMN+eWys=</pin>
        </pin-set>
    </domain-config>
    
    <!-- Debug configuration: Allow inspection tools during development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
